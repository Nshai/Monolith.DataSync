// File globs.

var path = require( 'path' );
var pathToThisFile = __dirname;
var root = path.dirname( pathToThisFile );
var destination = root + '/build/';

module.exports =
{
	// Microservice. Update this to match your microservice.
	// Make sure that solution and exe file are matched to this
	microservice: 'Microservice.Workflow',
	msbuild : {configuration: 'Debug'},
	
	// uncomment to use different gulp tools version
	//tasks : { nuget : { version : '0.0.12' }} 
};